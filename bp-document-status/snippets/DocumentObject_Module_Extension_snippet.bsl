' МОДУЛЬ ОБЪЕКТА Документ.РеализацияТоваровУслуг В РАСШИРЕНИИ — только этот файл (три &После).
' НЕ вставляйте сюда код из Form_Module_snippet.bsl (РеквизитФормыВЗначение — только в модуле формы).
' Строка в &После("...") должна совпадать с именем типовой процедуры (проверьте в основной конфигурации).

#Область СтатусДокументаРасширение

&После("ПередЗаписью")
Процедура бпСтатус_ПослеПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	ДокументыСтатусыОпераций.ПередЗаписьюДокументаСоСтатусом(Отказ, ЭтотОбъект, "РеализацияТоваровУслуг", РежимЗаписи);
КонецПроцедуры

&После("ПередУдалением")
Процедура бпСтатус_ПослеПередУдалением(Отказ)
	ДокументыСтатусыОпераций.ПередУдалениемДокументаСоСтатусом(Отказ, ЭтотОбъект, "РеализацияТоваровУслуг");
КонецПроцедуры

&После("ОбработкаЗаполнения")
Процедура бпСтатус_ПослеОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	ДокументыСтатусыОпераций.УстановитьСтатусПоУмолчаниюПриЗаполнении(ЭтотОбъект);
КонецПроцедуры

#КонецОбласти
