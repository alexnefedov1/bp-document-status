' ВСТАВКА В МОДУЛЬ ОБЪЕКТА Документ.РеализацияТоваровУслуг (не копируйте целиком, если процедуры уже существуют).

'--- ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения) ---
ДокументыСтатусыОпераций.ПередЗаписьюДокументаСоСтатусом(Отказ, ЭтотОбъект, "РеализацияТоваровУслуг", РежимЗаписи);
Если Отказ Тогда
	Возврат;
КонецЕсли;

'--- ПередУдалением(Отказ) ---
ДокументыСтатусыОпераций.ПередУдалениемДокументаСоСтатусом(Отказ, ЭтотОбъект, "РеализацияТоваровУслуг");
Если Отказ Тогда
	Возврат;
КонецЕсли;

'--- ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка) — в конец после типового кода ---
ДокументыСтатусыОпераций.УстановитьСтатусПоУмолчаниюПриЗаполнении(ЭтотОбъект);
