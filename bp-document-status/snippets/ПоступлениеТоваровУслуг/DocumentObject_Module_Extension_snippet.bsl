' МОДУЛЬ ОБЪЕКТА Документ.ПоступлениеТоваровУслуг В РАСШИРЕНИИ — только этот файл (три &После).
' НЕ вставляйте сюда код из Form_Module_snippet.bsl.

#Область СтатусДокументаРасширение

&После("ПередЗаписью")
Процедура бпСтатус_ПослеПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	ДокументыСтатусыОпераций.ПередЗаписьюДокументаСоСтатусом(Отказ, ЭтотОбъект, "ПоступлениеТоваровУслуг", РежимЗаписи);
КонецПроцедуры

&После("ПередУдалением")
Процедура бпСтатус_ПослеПередУдалением(Отказ)
	ДокументыСтатусыОпераций.ПередУдалениемДокументаСоСтатусом(Отказ, ЭтотОбъект, "ПоступлениеТоваровУслуг");
КонецПроцедуры

&После("ОбработкаЗаполнения")
Процедура бпСтатус_ПослеОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	ДокументыСтатусыОпераций.УстановитьСтатусПоУмолчаниюПриЗаполнении(ЭтотОбъект);
КонецПроцедуры

#КонецОбласти
