' ВСТАВКА В МОДУЛЬ ОБЪЕКТА Документ.ПоступлениеТоваровУслуг (не копируйте целиком, если процедуры уже существуют).

'--- ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения) ---
ДокументыСтатусыОпераций.ПередЗаписьюДокументаСоСтатусом(Отказ, ЭтотОбъект, "ПоступлениеТоваровУслуг", РежимЗаписи);
Если Отказ Тогда
	Возврат;
КонецЕсли;

'--- ПередУдалением(Отказ) ---
ДокументыСтатусыОпераций.ПередУдалениемДокументаСоСтатусом(Отказ, ЭтотОбъект, "ПоступлениеТоваровУслуг");
Если Отказ Тогда
	Возврат;
КонецЕсли;

'--- ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка) — в конец после типового кода ---
ДокументыСтатусыОпераций.УстановитьСтатусПоУмолчаниюПриЗаполнении(ЭтотОбъект);
